{
    // clangd config
	//is this necessary for coc-clangd to work ?? :CocRestart to check
    //"languageserver": {
    //      "clangd": {
    //          "enable": true,
    //          "command": "clangd",
    //          "rootPatterns": ["compile_commands.json", ".vim/", ".git/", ".hg/"],
    //          "filetypes": ["c", "cpp", "objc", "objcpp"],
    //          "args": ["-background-index"]
    //      }
    // },
    //required by vim-lsp-cxx-highlight
    "clangd.semanticHighlighting": true,
	
	//eslint config
	"eslint.trace.server": "verbose", // for debugging
	"eslint.packageManager": "npm", // has effect only when eslint is resolved globally 
	"eslint.codeAction.disableRuleComment": {
		// show disable lint rule in the quick fix menu
		"enable": true, // TO disable
		"location": "separateLine"
	},
	"eslint.codeAction.showDocumentation": {
		// show open lint rule documentation web page in the quick fix
		"enable": true
	}
}
